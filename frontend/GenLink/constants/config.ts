export const BASE_URL = 'https://hackheroes-2025-frontend.onrender.com/appviews';
// Force navigation/status bars to light white theme even if device/browser theme is dark.
// Set to true to override automatic theme adaptations and force white system bars.
export const FORCE_LIGHT_SYSTEM_UI = false;

// Force navigation bar background to white (useful on some OEMs). This will
// attempt to set the navigation bar background and position even if edge-to-edge
// is enabled. Use with caution — device might override this or log warnings.
export const FORCE_NAVIGATION_BAR_WHITE = false;
